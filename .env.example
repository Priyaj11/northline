# Copy this file to .env and adjust if needed:  cp .env.example .env
# .env is git ignored, so real values never reach the repository.

NORTHLINE_ENV=local
NORTHLINE_RELEASE=R1.0

# System Under Test (ParaBank)
SUT_BASE_URL=http://localhost:8080/parabank
SUT_USERNAME=john
SUT_PASSWORD=demo

# Certification data store (PostgreSQL)
PGHOST=localhost
PGPORT=5433
PGDATABASE=northline
PGUSER=northline
PGPASSWORD=northline
