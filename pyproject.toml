[project]
name = "northline"
version = "0.1.0"
description = "Enterprise banking release certification framework"
requires-python = ">=3.11"

[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
