# Requirements

Generated by scripts/generate_requirements_doc.py from requirements.yaml.
Do not edit by hand. Edit the YAML register and regenerate.

Release: R1.0  
System Under Test: ParaBank

Total requirements: 40

## Where these requirements come from

ParaBank is a demonstration application with no published specification, so
every requirement records its origin. This matters when a test fails: an
assumed requirement that fails may be a defect in the application, or it may
be a wrong assumption on our part. Recording the difference up front stops
assumptions being reported as defects.

| Source | Meaning | Count |
| --- | --- | --- |
| observed | verified directly against the running application in Phase 1 | 12 |
| derived | standard banking behaviour any reasonable specification includes | 22 |
| assumed | expected but unconfirmed; the test will settle it | 6 |

## Priority distribution

| Priority | Count |
| --- | --- |
| Critical | 17 |
| High | 17 |
| Medium | 6 |
| Low | 0 |

## Requirements by area

### ENV - Environment readiness

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-ENV-001 | The application home page must be reachable | Critical | observed | environment |
| REQ-ENV-002 | The application must be provisioned before testing | Critical | observed | environment |
| REQ-ENV-003 | The served application must be ParaBank | High | observed | environment |
| REQ-ENV-004 | The application database schema must exist | Critical | observed | environment |
| REQ-ENV-005 | The certification data store must accept connections | Critical | observed | environment |

**REQ-ENV-001** A GET of the home page returns HTTP 200.

**REQ-ENV-002** The home page returns 200 with redirects disabled. A 302 to the database initialisation page means the schema and demo data are missing.

**REQ-ENV-003** The home page content identifies the application, not a default server page.

**REQ-ENV-004** The admin page returns 200. That page queries the Parameter table, so a 200 is positive evidence that the schema was created.

**REQ-ENV-005** PostgreSQL accepts a connection and answers a trivial query.

### AUTH - Authentication and session

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-AUTH-001 | A registered customer can log in with valid credentials | Critical | observed | ui, api |
| REQ-AUTH-002 | Login with invalid credentials is rejected | Critical | derived | ui, api |
| REQ-AUTH-003 | Login with empty credentials is rejected | High | derived | ui |
| REQ-AUTH-004 | A logged-in customer can log out | High | derived | ui |
| REQ-AUTH-005 | A new customer can register | High | assumed | ui, api |

**REQ-AUTH-001** Valid username and password grant access to the account overview.

**REQ-AUTH-002** A wrong username, a wrong password, or both must be refused, and the error message must not reveal which of the two was wrong.

**REQ-AUTH-003** An empty username or password is refused with a validation message.

**REQ-AUTH-004** Logging out ends the session and returns the customer to the login page.

**REQ-AUTH-005** Registration creates a customer with an opening account. Needed so tests can create their own data instead of competing for the shared demo user.

### ACCT - Accounts

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-ACCT-001 | A customer can view their list of accounts | Critical | observed | ui, api |
| REQ-ACCT-002 | A customer can view the detail of a single account | High | observed | ui, api |
| REQ-ACCT-003 | Account balances shown in the interface match the system of record | Critical | derived | ui, api, database |
| REQ-ACCT-004 | A customer can open a new account | Medium | assumed | ui, api |

**REQ-ACCT-001** The overview lists every account belonging to the customer with its balance.

**REQ-ACCT-002** Account detail shows identifier, type and balance.

**REQ-ACCT-003** The balance rendered in the browser equals the balance returned by the service and the value recorded in the certification data store.

**REQ-ACCT-004** A new account can be opened, funded from an existing account.

### XFER - Funds transfer

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-XFER-001 | A customer can transfer funds between their own accounts | Critical | observed | ui, api, database |
| REQ-XFER-002 | A transfer debits the source and credits the destination by the same amount | Critical | derived | api, database |
| REQ-XFER-003 | A transfer of zero or a negative amount is rejected | High | assumed | ui, api |
| REQ-XFER-004 | A transfer with a non-numeric or malformed amount is rejected | High | assumed | ui, api |
| REQ-XFER-005 | A transfer to a non-existent destination account is rejected | High | assumed | ui, api, database |
| REQ-XFER-006 | A completed transfer produces a transaction record on both accounts | Critical | derived | ui, api, database |

**REQ-XFER-001** A valid transfer moves the stated amount from one account to another.

**REQ-XFER-002** After a transfer of amount A, the source balance falls by exactly A and the destination balance rises by exactly A. No money is created or lost.

**REQ-XFER-003** Amounts at or below zero are not valid transfer amounts.

**REQ-XFER-004** Text, symbols and excessive decimal precision are refused with a validation message.

**REQ-XFER-005** An unknown destination account identifier does not result in a completed transfer.

**REQ-XFER-006** Each side of the transfer appears in that account's transaction history.

### BILL - Bill payment

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-BILL-001 | A customer can pay a bill from an account | High | observed | ui, api, database |
| REQ-BILL-002 | Bill payment rejects incomplete or invalid payee details | Medium | assumed | ui |
| REQ-BILL-003 | A completed bill payment produces a transaction record | High | derived | api, database |

**REQ-BILL-001** A valid bill payment debits the chosen account by the stated amount.

**REQ-BILL-002** Missing required payee fields are refused with validation messages.

**REQ-BILL-003** The payment appears in the source account's transaction history.

### TXN - Transaction history

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-TXN-001 | A customer can view an account's transaction history | High | observed | ui, api |
| REQ-TXN-002 | Transaction history can be filtered | Medium | observed | api |
| REQ-TXN-003 | A transaction record carries account, amount, type and date | High | derived | api, database |

**REQ-TXN-001** The history lists transactions for the selected account.

**REQ-TXN-002** History can be searched by amount and by date range.

**REQ-TXN-003** Every transaction returned includes those four fields with correct types.

### DATA - Data integrity and reconciliation

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-DATA-001 | Transaction data can be extracted into the certification data store | Critical | derived | database |
| REQ-DATA-002 | A settlement file can be produced from transaction data | Critical | derived | reconciliation |
| REQ-DATA-003 | Reconciliation detects every class of break | Critical | derived | reconciliation |
| REQ-DATA-004 | Reconciliation produces a report with a pass or fail status | High | derived | reconciliation |

**REQ-DATA-001** Accounts and transactions are extracted from the service into PostgreSQL.

**REQ-DATA-002** A settlement file is generated with transaction identifier, account, amount, type and timestamp.

**REQ-DATA-003** Comparing the settlement file against the data store detects missing records, duplicate records, amount mismatches and account mismatches.

**REQ-DATA-004** Totals, matched, missing, duplicates, mismatches and an overall status.

### SEC - Security-adjacent behaviour

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-SEC-001 | Customer financial data must require authentication | Critical | derived | security, api |
| REQ-SEC-002 | A customer cannot access another customer's accounts | Critical | derived | security, api |
| REQ-SEC-003 | Credentials must not be transmitted in a URL path | High | derived | security |
| REQ-SEC-004 | Restricted pages are not reachable without a session | Critical | derived | security, ui |

**REQ-SEC-001** Reading account or transaction data must require an authenticated session. Phase 1 observed the REST services returning full account data with no credentials supplied.

**REQ-SEC-002** Requesting an account belonging to another customer must be refused.

**REQ-SEC-003** URLs are recorded in access logs, browser history and proxy logs. Phase 1 observed a login endpoint taking username and password as path segments.

**REQ-SEC-004** Requesting an authenticated page directly without logging in does not return customer data.

### ACC - Accessibility

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-ACC-001 | Key pages have no critical or serious automated accessibility violations | High | derived | accessibility |
| REQ-ACC-002 | Every form input has a programmatically associated label | High | derived | accessibility |
| REQ-ACC-003 | Key workflows are operable by keyboard alone | High | derived | accessibility |

**REQ-ACC-001** Login, account overview and transfer pages pass an automated axe-core scan at WCAG 2.1 level AA for critical and serious impact rules.

**REQ-ACC-002** Screen reader users must be able to identify each field.

**REQ-ACC-003** Login and transfer can be completed with keyboard only, with a visible focus indicator and a sensible tab order. Verified manually, because automated tools cannot confirm this.

### PERF - Performance

| ID | Title | Priority | Source | Verified by |
| --- | --- | --- | --- | --- |
| REQ-PERF-001 | Login responds within the agreed threshold under load | Medium | derived | performance |
| REQ-PERF-002 | Account retrieval responds within the agreed threshold under load | Medium | derived | performance |
| REQ-PERF-003 | The error rate under load stays within the agreed threshold | Medium | derived | performance |

**REQ-PERF-001** 95th percentile response time stays under the configured limit.

**REQ-PERF-002** 95th percentile response time stays under the configured limit.

**REQ-PERF-003** The proportion of failed requests stays under the configured limit.
